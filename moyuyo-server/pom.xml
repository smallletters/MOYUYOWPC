<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>3.5.16</version>
        <relativePath/>
    </parent>

    <groupId>com.moyuyo</groupId>
    <artifactId>moyuyo-server</artifactId>
    <version>1.0.0</version>
    <packaging>pom</packaging>
    <name>MOYUYO Server</name>
    <description>MOYUYO 宠物用品商城后端服务</description>

    <modules>
        <module>moyuyo-common</module>
        <module>moyuyo-dao</module>
        <module>moyuyo-service</module>
        <module>moyuyo-api</module>
    </modules>

    <properties>
        <java.version>25</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <mybatis-plus.version>3.5.14</mybatis-plus.version>
        <jjwt.version>0.12.6</jjwt.version>
        <springdoc.version>2.8.4</springdoc.version>
        <lombok.version>1.18.42</lombok.version>
        <flyway.version>10.22.0</flyway.version>
        <owasp-html-sanitizer.version>20240325.1</owasp-html-sanitizer.version>
        <owasp-dependency-check.version>11.1.0</owasp-dependency-check.version>
        <caffeine.version>3.1.8</caffeine.version>
        <logstash-logback.version>8.0</logstash-logback.version>
        <!-- Stripe 官方 Java SDK，封装 Checkout Session / Webhook 验签 / PaymentIntent 等 API -->
        <stripe.version>28.1.0</stripe.version>
        <!-- RocketMQ：starter 与 client 版本必须严格对齐。starter 2.x 自带 client 5.x 依赖，
             这里显式声明 client 版本是为了让 IDE 与 CI 看清楚运行时依赖 -->
        <rocketmq.version>2.3.1</rocketmq.version>
        <rocketmq-client.version>5.3.2</rocketmq-client.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- 内部模块 -->
            <dependency>
                <groupId>com.moyuyo</groupId>
                <artifactId>moyuyo-common</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.moyuyo</groupId>
                <artifactId>moyuyo-dao</artifactId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <groupId>com.moyuyo</groupId>
                <artifactId>moyuyo-service</artifactId>
                <version>${project.version}</version>
            </dependency>

            <!-- MyBatis-Plus -->
            <dependency>
                <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-spring-boot3-starter</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser</artifactId>
            <version>${mybatis-plus.version}</version>
        </dependency>

            <!-- JWT -->
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-api</artifactId>
                <version>${jjwt.version}</version>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-impl</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>
            <dependency>
                <groupId>io.jsonwebtoken</groupId>
                <artifactId>jjwt-jackson</artifactId>
                <version>${jjwt.version}</version>
                <scope>runtime</scope>
            </dependency>

            <!-- SpringDoc OpenAPI -->
            <dependency>
                <groupId>org.springdoc</groupId>
                <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
                <version>${springdoc.version}</version>
            </dependency>

            <!-- Flyway 数据库迁移 -->
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-core</artifactId>
                <version>${flyway.version}</version>
            </dependency>
            <dependency>
                <groupId>org.flywaydb</groupId>
                <artifactId>flyway-mysql</artifactId>
                <version>${flyway.version}</version>
            </dependency>

            <!-- RocketMQ（starter 与 client 版本必须严格对齐，否则启动失败）-->
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-spring-boot-starter</artifactId>
                <version>${rocketmq.version}</version>
            </dependency>
            <dependency>
                <groupId>org.apache.rocketmq</groupId>
                <artifactId>rocketmq-client</artifactId>
                <version>${rocketmq-client.version}</version>
            </dependency>

            <!-- Resilience4j 限流熔断 -->
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-spring-boot3</artifactId>
                <version>2.2.0</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-retry</artifactId>
                <version>2.2.0</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-circuitbreaker</artifactId>
                <version>2.2.0</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-ratelimiter</artifactId>
                <version>2.2.0</version>
            </dependency>
            <dependency>
                <groupId>io.github.resilience4j</groupId>
                <artifactId>resilience4j-bulkhead</artifactId>
                <version>2.2.0</version>
            </dependency>
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-starter-aop</artifactId>
                <version>3.5.16</version>
            </dependency>

            <!-- OWASP HTML Sanitizer：用于净化用户提交的富文本，防止存储型 XSS -->
            <dependency>
                <groupId>com.googlecode.owasp-java-html-sanitizer</groupId>
                <artifactId>owasp-java-html-sanitizer</artifactId>
                <version>${owasp-html-sanitizer.version}</version>
            </dependency>

            <!-- Caffeine 本地缓存（进程内 LRU 缓存，读多写少场景）-->
            <dependency>
                <groupId>com.github.ben-manes.caffeine</groupId>
                <artifactId>caffeine</artifactId>
                <version>${caffeine.version}</version>
            </dependency>

            <!-- Prometheus 指标采集 -->
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-registry-prometheus</artifactId>
                <version>1.14.5</version>
            </dependency>

            <!-- Brave/Micrometer Tracing bridge：traceId 跨线程透传 + 响应头 X-Trace-Id -->
            <dependency>
                <groupId>io.micrometer</groupId>
                <artifactId>micrometer-tracing-bridge-brave</artifactId>
                <!-- 1.14.5 在 Maven Central 不存在（实际最新 stable 为 1.5.x），降到 1.5.12 以通过全新依赖解析 -->
                <version>1.5.12</version>
            </dependency>

            <!-- Logstash 日志编码器（结构化日志 + MDC 输出 JSON）-->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash-logback.version}</version>
            </dependency>

            <!-- OWASP Dependency-Check 漏洞扫描 -->
            <dependency>
                <groupId>org.owasp</groupId>
                <artifactId>dependency-check-maven</artifactId>
                <version>${owasp-dependency-check.version}</version>
                <type>maven-plugin</type>
            </dependency>

            <!-- Stripe Java SDK（H1 修复：改用 Stripe.checkout.Session 替代手写 PaymentIntent） -->
            <dependency>
                <groupId>com.stripe</groupId>
                <artifactId>stripe-java</artifactId>
                <version>${stripe.version}</version>
            </dependency>

        </dependencies>
    </dependencyManagement>

    <build>
        <!--
          Maven Compiler 全局配置：显式声明 Lombok 注解处理器
          解决 JDK 9+ 后 javac 默认不再从 classpath 扫描注解处理器的问题
          （Maven 3.9+ 也会强制要求显式声明，否则 lombok.* 注解全部失效、Slf4j log 字段为 null）
          这里写在 pluginManagement 外面的 build 块，确保所有子模块默认继承（pluginManagement 仅在子模块显式声明插件时才生效）
        -->
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <configuration>
                    <source>${java.version}</source>
                    <target>${java.version}</target>
                    <!-- P1 修复：maven-compiler-plugin 3.11+ 使用 inputEncoding，
                         旧版使用 encoding。两者兼容设置，覆盖 Windows 默认 GBK 行为。
                         中文源码/注释会被 javac 正确按 UTF-8 解析，避免乱码编译错误 -->
                    <inputEncoding>${project.build.sourceEncoding}</inputEncoding>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <fork>true</fork>
                    <!-- 显式传递 -Dfile.encoding=UTF-8 给 javac 子进程，
                         确保 Windows 上 fork 出来的 javac 也使用 UTF-8（默认 GBK） -->
                    <compilerArgs>
                        <arg>-J-Dfile.encoding=UTF-8</arg>
                    </compilerArgs>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                            <version>${lombok.version}</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>
        </plugins>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>org.apache.maven.plugins</groupId>
                    <artifactId>maven-checkstyle-plugin</artifactId>
                    <version>3.4.0</version>
                    <configuration>
                        <configLocation>checkstyle.xml</configLocation>
                        <consoleOutput>true</consoleOutput>
                        <!-- 当前 checkstyle 配置为 warning 级别（severity=warning），
                             保持 failsOnError=false 避免历史遗留问题阻塞 CI。
                             后续可在 checkstyle.xml 中将关键规则升级为 error 后再切 true -->
                        <failsOnError>false</failsOnError>
                    </configuration>
                    <executions>
                        <execution>
                            <phase>validate</phase>
                            <goals>
                                <goal>check</goal>
                            </goals>
                        </execution>
                    </executions>
                </plugin>
                <!--
                  OWASP Dependency-Check：扫描 Maven 依赖中的已知 CVE 漏洞
                  使用方法：mvn -pl moyuyo-api org.owasp:dependency-check-maven:check
                  报告：target/dependency-check-report.html
                  阈值：CVSS >= 7 视为高危，会令 build 失败（failBuildOnAnyVulnerability=true）
                  注意：首次运行需下载 NVD 数据集，可能耗时较长
                -->
                <plugin>
                    <groupId>org.owasp</groupId>
                    <artifactId>dependency-check-maven</artifactId>
                    <version>${owasp-dependency-check.version}</version>
                    <configuration>
                        <!--
                          failBuildOnAnyVulnerability 与 failBuildOnCVSS 是 OR 关系，
                          两者同时开启时：任何已知漏洞都失败且 CVSS 阈值不生效。
                          这里只保留 failBuildOnCVSS，由 CVSS 阈值（>= 7）决定是否阻断构建。
                        -->
                        <failBuildOnAnyVulnerability>false</failBuildOnAnyVulnerability>
                        <failBuildOnCVSS>7</failBuildOnCVSS>
                        <suppressionFiles>
                            <suppressionFile>dependency-check-suppressions.xml</suppressionFile>
                        </suppressionFiles>
                        <!-- NVD 数据镜像，国内服务器加速 -->
                        <dataDirectory>${project.build.directory}/dependency-check-data</dataDirectory>
                        <skipProvidedScope>true</skipProvidedScope>
                        <skipRuntimeScope>false</skipRuntimeScope>
                        <skipTestScope>true</skipTestScope>
                        <!-- 抑制 NVD 数据集下载失败（国内网络偶发 NVD 拉取失败不影响构建） -->
                        <autoUpdate>false</autoUpdate>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>
    </build>
</project>
