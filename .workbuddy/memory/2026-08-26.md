## moyuyo-app UI 图标审计（仅建议，未改动代码）
- 项目：uni-app + uview-plus 轻奢宠物电商（MOYUYO ATELIER，主色 #DBC98A / 文字 #2E2B29）
- 图标现状问题：
  1. emoji 当图标用（📦🔍💳等，遍布 user/pet/order/goods 等几十个页面模板）
  2. Unicode 字符当图标（‹ › ✓ ✕ × ★ 等，约 200 处，60 处 back-icon 用 ‹）
  3. 7 个页面（bargain/charity-donation/crowdfunding/frequent-purchase/group-buy/live-room/invoice）用 iconfont icon-xxx 类但全项目无 iconfont 字体与 CSS → 图标空白
  4. TabBar 用 Lucide 风格 SVG（static/images/tabbar/*.svg），mp-weixin/App 原生 tabBar 不支持 SVG，需 PNG
  5. uview-plus u-icon 仅用约 6 处
- 建议方向：统一为一套 1.5px 线性 iconfont（Lucide/Phosphor 子集 TTF），未选中 #9A948C、选中 #DBC98A；emoji 全部替换为单色图标；tabBar SVG 导出 PNG

## moyuyo-app 图标迁移实施（进行中）
- 已生成 Lucide 子集图标字体：`moyuyo-app/static/fonts/lucide.ttf`（38KB）+ `src/styles/lucide.scss`（142 个图标类 .luc-*）
- Pass 1（replace_icons.py）已跑完：124 个 .vue 文件中的静态 iconfont 类 + emoji/unicode 被替换为 `<text class="luc luc-xxx"></text>`
- 已在 `App.vue` 全局引入 `lucide.scss`，使 luc-* 类生效
- 剩余工作：模板中仍有 155 个 emoji（多为动态绑定如 `{{ state.icon }}`），需 replace_dynamic.py 处理；tabBar SVG 需转 PNG；uni-app 编译/真机验证
