# 依赖
node_modules/
.pnp
.pnp.js

# 构建产物
dist/
unpackage/
.hbuilderx/

# 环境变量（包含 API 凭证，禁止提交）
.env
.env.local
.env.*.local
# 防止把 .env.development / .env.production 等带环境后缀的本地配置误提交
.env.*

# 调试日志
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# 编辑器
.vscode/
.idea/
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# 系统
.DS_Store
Thumbs.db

# 临时文件
*.tmp
*.temp

# Maven 构建产物
**/target/

# 运行日志
logs/
moyuyo-server/logs/
moyuyo-server/moyuyo-api/logs/
moyuyo-server/*.log

# Gradle 缓存
**/.gradle/

# 管理后台前端构建产物
moyuyo-server/moyuyo-api/src/main/resources/static/admin/

# Trae 计划文档
.trae/

# 服务器错误日志
moyuyo-server/server.err

# 根目录运行日志
server.log
server_err.log
server_out.log
server-out.log
server-start.log
start-backend.ps1
backend.log
ports.log

# 本地启动脚本输出的实时日志（dev 后端 / dev admin 启动时产生，进程锁住不要提交）
# （*.log 在前面已经匹配到，这里只是注释说明）

# Spring Boot 内嵌打包产物（应通过 mvn package 重新生成）
**/BOOT-INF/

# 本地调试响应 / Token 抓包
admin-token.json
*-token.json

# Edge 浏览器调试目录
.edge-debug-profile/

# 本地测试脚本
test_*.ps1
test_*.sh
add_*.sql
cleanup_*.sql
check_*.mjs

# moyuyo-app 前端调试日志（含 token / 调试输出，禁止提交）
moyuyo-app/*.log
moyuyo-app/**/*.log

# 本地调试 / 探针 / 抓包产物
probe*.ps1
probe*.sh
*_oneline.txt

# 超大 3D 模型（299MB，超出 GitHub 单文件 100MB 限制，改用 LFS / CDN 分发）
moyuyo-app/src/static/models/modern_apartment.glb
